<nav>
    <div class="nav-left">
        <div class="header inline"><%= link_to 'HOME IMPROVEMENT', root_path, style: 'text-decoration: none;'%></div>
        <% if current_user %>
            <div class="new_project inline" id="new_project_button">
        <% end %>
        </div>
    </div>
    <div class="nav-right flex-centering"> 
        <% if user_signed_in? %>
            <%=link_to "Sign out",destroy_user_session_path, method:"delete", class:'auth-button flex-centering uppercase', wave: 'light'%>
        <% else %>
            <%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path, class:'auth-button flex-centering uppercase' %>
        <% end %>
    </div>
</nav>

<script>

$(document).ready(function(){
    if(document.getElementById('new_project_button')){
        ReactOnRails.render('NewProject', {}, 'new_project_button');
    }
})

</script>